services:
  - type: web
    name: inventory-comparer
    runtime: node
    rootDir: inventory-comparer
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
